{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Introduction","text":"<p>From early 2018 until late 2022 I worked as a software developer attached to a bioinformatics team. One of the things I saw the team around me doing was a lot of \"scoring\" of sequences, etc (imagine a lot of layman hand-waving here, obviously).</p> <p>Playing on the \"DNA is just code\" nonsense trope, it became a bit of a running joke for me to suggest that they should try a scoring system based on a popular tile-based word game. Eventually, with a bit of spare time on my hands, I decided to actually write and publish a library that implemented this.</p> <p>And so the Flounder Score was born.</p> <p>No, it's not a serious library, but I also think it makes a serious point about people who think \"it's just code\".</p>"},{"location":"#installation","title":"Installation","text":"<p>The library can be installed from PyPI:</p> <pre><code>$ pip install flounder-score\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<p>See the module documentation for the detailed library documentation.</p>"},{"location":"#getting-the-score","title":"Getting the score","text":"<p>This function takes a string that is a sequence returns an integer value that is the flounder score for that sequence. Case is not important. IUAPC codes are taken into account.</p> <p>Example:</p> SourceResult <pre><code>from flounder import score\n\nprint(score(\"GTAC\"))\n</code></pre> <pre><code>7\n</code></pre>"},{"location":"#getting-all-the-scores","title":"Getting all the scores","text":"<p>This function takes a string that is a sequence, and returns a list of tuples. Each tuple is the base at that position in the input string, along with its individual <code>score</code>.</p> <p>Example:</p> SourceResult <pre><code>from flounder import scores\n\nprint(scores(\"gtac\"))\n</code></pre> <pre><code>[('g', 2), ('t', 1), ('a', 1), ('c', 3)]\n</code></pre>"},{"location":"#getting-the-score-to-the-max","title":"Getting the score to the max","text":"<p>This function takes a string that is a sequence, and returns a \"to the max\" score. Case is not important. IUAPC codes are taken into account.</p> <p>The difference with this scoring is that, for the bases, not only do they score for their own <code>score</code>, they also <code>score</code> for every IUAPC code that is related to that base.</p> <p>Example:</p> SourceResult <pre><code>from flounder import score_to_the_max\n\nprint(score_to_the_max(\"gtac\"))\n</code></pre> <pre><code>86\n</code></pre>"},{"location":"#getting-all-the-scores-to-the-max","title":"Getting all the scores to the max","text":"<p>This function is similar to <code>scores</code>, except it returns a list of tuples where each tuple is the base at that position in the input sequence, along with its individual <code>score_to_the_max</code>.</p> <p>Example:</p> SourceResult <pre><code>from flounder import scores_to_the_max\n\nprint(scores_to_the_max(\"gtac\"))\n</code></pre> <pre><code>[('g', 19), ('t', 24), ('a', 20), ('c', 23)]\n</code></pre>"},{"location":"#getting-the-codon-score","title":"Getting the codon score","text":"<p>This function takes the input sequence, translates it into an amino acid sequence (using as many codons as it can find from the first position), and then scores that resulting sequence.</p> <p>For example:</p> SourceResult <pre><code>from flounder import codon_score\n\nprint(codon_score(\"AGACGCAGTCTT\"))\n</code></pre> <pre><code>4\n</code></pre>"},{"location":"#getting-the-codon-scores","title":"Getting the codon scores","text":"<p>This function takes an input sequence, translates it into an amino acid sequence (using as many codons as it can find from the first position), and then returns a list of tuples where the value in the first position is the codon being scored and the second is the score for that codon.</p> <p>For example:</p> SourceResult <pre><code>from flounder import codon_scores\n\nprint(codon_scores(\"AGACGCAGTCTT\"))\n</code></pre> <pre><code>[('AGA', 1), ('CGC', 1), ('AGT', 1), ('CTT', 1)]\n</code></pre>"},{"location":"changelog/","title":"Flounder Score ChangeLog","text":""},{"location":"changelog/#unreleased","title":"Unreleased","text":"<p>Released: WiP</p> <ul> <li>Performed a complete overhaul of the package (build environment, packaging   tool, metadata, etc).   (#6)</li> </ul>"},{"location":"changelog/#v120","title":"v1.2.0","text":"<p>Released: 2021-10-28</p> <ul> <li>Tidied up and made available on pypi.</li> </ul>"},{"location":"changelog/#v110","title":"v1.1.0","text":"<p>Released: 2019-12-20</p> <ul> <li>Added <code>codon_score</code>.</li> <li>Added <code>codon_scores</code>.</li> </ul>"},{"location":"changelog/#v100","title":"v1.0.0","text":"<p>Released: 2019-12-18</p> <p>Initial release.</p>"},{"location":"flounder/","title":"Flounder","text":"<p>Tools to score a DNA sequence using the Flounder method.</p>"},{"location":"flounder/#flounder.score.codon_score","title":"codon_score","text":"<pre><code>codon_score(sequence)\n</code></pre> <p>Return the codon-based Flounder Score for the given sequence.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The score for the sequence.</p> <p>This scoring system translates the codons in the sequence into AA codes, and then builds a score based on them. Translation of the sequence always starts with the first base, and keeps going as long as there are codons left. Stop codons are scored as 0 and worked past.</p>"},{"location":"flounder/#flounder.score.codon_score(sequence)","title":"<code>sequence</code>","text":""},{"location":"flounder/#flounder.score.codon_scores","title":"codon_scores","text":"<pre><code>codon_scores(sequence)\n</code></pre> <p>Return a list of codon-based Flounder Scores for the given sequence.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>list[tuple[str, int]]</code> <p>A list of tuples, the codon and its score.</p> <p>This scoring system translates the codons in the sequence into AA codes, and then builds a score based on them. Translation of the sequence always starts with the first base, and keeps going as long as there are codons left. Stop codons are scored as 0 and worked past.</p>"},{"location":"flounder/#flounder.score.codon_scores(sequence)","title":"<code>sequence</code>","text":""},{"location":"flounder/#flounder.score.score","title":"score","text":"<pre><code>score(sequence)\n</code></pre> <p>Return the flounder score for the given sequence.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The flounder score.</p>"},{"location":"flounder/#flounder.score.score(sequence)","title":"<code>sequence</code>","text":""},{"location":"flounder/#flounder.score.score_to_the_max","title":"score_to_the_max","text":"<pre><code>score_to_the_max(sequence)\n</code></pre> <p>Return the to-the-max Flounder Score for the given sequence.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The flounder score for the sequence.</p> <p>The to-the-max flounder score is like the flounder score, but other than scoring the individual base, it scores based on the base and also every IUAPC code that applies.</p>"},{"location":"flounder/#flounder.score.score_to_the_max(sequence)","title":"<code>sequence</code>","text":""},{"location":"flounder/#flounder.score.scores","title":"scores","text":"<pre><code>scores(sequence)\n</code></pre> <p>Return a list of individual bases with their scores from the sequence.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>list[tuple[str, int]]</code> <p>A list of tuples, the base in the first position, the <code>score</code> in the second.</p>"},{"location":"flounder/#flounder.score.scores(sequence)","title":"<code>sequence</code>","text":""},{"location":"flounder/#flounder.score.scores_to_the_max","title":"scores_to_the_max","text":"<pre><code>scores_to_the_max(sequence)\n</code></pre> <p>Return a list of individual base to-the-max scores.</p> <p>Parameters:</p> Name Type Description Default <code>str</code> <p>The sequence to score.</p> required <p>Returns:</p> Type Description <code>list[tuple[str, int]]</code> <p>A list of tuples, the base in the first position, the <code>score</code> in the second.</p>"},{"location":"flounder/#flounder.score.scores_to_the_max(sequence)","title":"<code>sequence</code>","text":""}]}